import React from 'react'

function ManageInvoices() {
  return (
    <div>
      {/* Show orders with delivery status: pendig here(Current orders)
      # Before delivery then after delivery move to past invoices and show them all here */}
    </div>
  )
}

export default ManageInvoices
